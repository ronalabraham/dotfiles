# bash aliases

# ls aliases
alias ll='ls -alF'
alias la='ls -A'
alias l='ls -ltr'

# prevent accidental clobbering of files
alias cp='cp -i'
alias mv='mv -i'
alias rm='rm -i'

# redo command from history
alias r='fc -s'
