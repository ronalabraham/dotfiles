# bash aliases

# ls aliases
alias l='ls -ltr'
alias ll='ls -alF'

# prevent accidental clobbering of files
alias cp='cp -i'
alias mv='mv -i'
alias rm='rm -i'

# redo command from history
alias r='fc -s'
